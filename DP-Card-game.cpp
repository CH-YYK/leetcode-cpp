// Two people A and B repeatedly pick cards out.
// and every time it can only pick 1 or 2 or 3

// The score for each one is determined by the sum of cards in hand
// compute the maximum value A can get after the game.
#include <vector>
#include <iostream>

using namespace std;


class Solution {
    // recursive solution
public:
    int maxScore(vector<int> &nums) {

    }

    int helper(vector<int>& nums) {

    }

};

int main() {
    int M = 4;
    int x = 1 << M;
    vector<char> nums(1 << M, 67);
    for(auto &i:nums) cout << i << " ";
    cout << "\n";
}

